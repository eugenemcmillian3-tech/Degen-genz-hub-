import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";
import { ResponseLogger } from "@/components/response-logger";
import { cookies } from "next/headers";
import { ReadyNotifier } from "@/components/ready-notifier";
import FarcasterWrapper from "@/components/FarcasterWrapper";
import { StructuredData } from "@/components/structured-data";
import { generateMetadata as genMeta } from "@/lib/seo";
import { AnalyticsProvider } from "@/components/analytics-provider";
import { OnchainProviders } from "@/components/providers/onchainkit-provider";
import { MiniKitProvider } from "@/hooks/use-minikit";
import "@/lib/base-account-ui.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  const requestId = cookies().get("x-request-id")?.value;

  return (
        <html lang="en">
          <head>
            {requestId && <meta name="x-request-id" content={requestId} />}
            <StructuredData type="WebApplication" />
            <link rel="preconnect" href="https://fonts.googleapis.com" />
            <link rel="dns-prefetch" href="https://api.openrouter.ai" />
            <link rel="dns-prefetch" href="https://supabase.co" />
            {/* Base Pay SDK for Base Builder payments */}
            <script src="https://unpkg.com/@base-org/account/dist/base-account.min.js" async />
          </head>
          <body
            className={`${geistSans.variable} ${geistMono.variable} antialiased`}
          >
            {/* Do not remove this component, we use it to notify the parent that the mini-app is ready */}
            <ReadyNotifier />
            
      <OnchainProviders>
        <MiniKitProvider>
          <AnalyticsProvider>
            <FarcasterWrapper>
              {children}
            </FarcasterWrapper>
          </AnalyticsProvider>
        </MiniKitProvider>
      </OnchainProviders>
      
            <ResponseLogger />
          </body>
        </html>
      );
}

export const metadata: Metadata = {
        title: "Degenz Hub",
        description: "Join Degenz Hub for AI-driven memecoins, NFTs, viral content, and trading insights. Affordable features, multi-chain payments, and engaging contests. All powered by AI for Gen Z!",
        other: { "fc:frame": JSON.stringify({
          "version": "next",
          "imageUrl": "https://usdozf7pplhxfvrl.public.blob.vercel-storage.com/thumbnail_b6c621e8-28bd-48ad-92ad-528085f6201f-QmfLIPDgmtvAFEmOixEHT9zrShjlpM",
          "button": {
            "title": "Open with Ohara",
            "action": {
              "type": "launch_frame",
              "name": "Degenz Gen/Z Hub",
              "url": "https://occasionally-began-383.app.ohara.ai",
              "splashImageUrl": "https://usdozf7pplhxfvrl.public.blob.vercel-storage.com/farcaster/splash_images/splash_image1.svg",
              "splashBackgroundColor": "#ffffff"
            }
          }
        }
        ) }
    };
